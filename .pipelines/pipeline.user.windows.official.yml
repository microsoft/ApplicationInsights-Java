version:
  major: 0
  minor: 0
  name: 'ApplicationInsights-Java'
  system: 'custom'
build:
  commands:
    - !!buildcommand
      name: 'CopyAndUploadSources'
      command: '.scripts/copy-source.cmd' # this file is empty
      artifacts:
        - from: "source-temp/" # source is checked out to C:/M/_work/3/b/46/_work/1/s/.git/
          to: 'Sources'
          include:
            - '**/*'
    - !!buildcommand
      name: 'Assemble ApplicationInsights-Java JARs'
      command: '.scripts/gradle.cmd'
      arguments: 'assemble copyLibsToGlobalArtifactsFolder -DisRelease=true -Pai.etw.native.build=release'
      artifacts:
        - to: 'Artifacts'
          include:
            - '**/build/libs/*.jar'
            - '**/build/libs/*-sources.jar'
            - '**/build/libs/*-javadoc.jar'
            - '**/build/libs/*.pom'
          exclude:
            - 'buildSrc/**/*'
